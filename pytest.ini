[pytest]
markers =
    slow: marks tests as slow (dataset integrity checks, skipped by default)
    db: requires a reachable TimescaleDB (docker-compose up -d)
filterwarnings =
    ignore::pandas.errors.PerformanceWarning
